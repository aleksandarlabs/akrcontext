---
type: akrctx-wiki-agent-setup
title: "Agent Setup"
description: "Readiness report for the akrctx harness."
tags: ["agent-setup", "doctor"]
timestamp: 2026-09-19T22:33:02.407Z
---

# Agent Setup

Agent readiness: 100/100

## Detected Targets

- codex
- claude

## Installed Targets

- codex
- claude

## Missing Files

- None

## Human-Approved Merge Needed

- None

## Suggested Safe Next Steps

- [info] Setup is complete. You can create a task capsule with `akrctx task "<description>"`.
- [warning] TASK-014: .akrctx/tasks/TASK-014-dogfooded-install-not-reproducible/log.md is inside the reviewed boundary, so the judge reads the implementing agent's own account as evidence. The implementation log belongs at .akrctx/local/impl/TASK-014/log.md. Doctor does not move it; decide whether to move it yourself.
- [warning] TASK-016: .akrctx/tasks/TASK-016-split-cli-monolith/log.md is inside the reviewed boundary, so the judge reads the implementing agent's own account as evidence. The implementation log belongs at .akrctx/local/impl/TASK-016/log.md. Doctor does not move it; decide whether to move it yourself.
- [warning] TASK-021: .akrctx/tasks/TASK-021-judge-record-shape/log.md is inside the reviewed boundary, so the judge reads the implementing agent's own account as evidence. The implementation log belongs at .akrctx/local/impl/TASK-021/log.md. Doctor does not move it; decide whether to move it yourself.
- [warning] TASK-022: .akrctx/tasks/TASK-022-snapshot-write-detection/log.md is inside the reviewed boundary, so the judge reads the implementing agent's own account as evidence. The implementation log belongs at .akrctx/local/impl/TASK-022/log.md. Doctor does not move it; decide whether to move it yourself.
- [warning] TASK-040: .akrctx/tasks/TASK-040-agent-spawn-discovery/log.md is inside the reviewed boundary, so the judge reads the implementing agent's own account as evidence. The implementation log belongs at .akrctx/local/impl/TASK-040/log.md. Doctor does not move it; decide whether to move it yourself.
- [warning] TASK-042: .akrctx/tasks/TASK-042-judge-schema-compliance/log.md is inside the reviewed boundary, so the judge reads the implementing agent's own account as evidence. The implementation log belongs at .akrctx/local/impl/TASK-042/log.md. Doctor does not move it; decide whether to move it yourself.
- [warning] TASK-044: .akrctx/tasks/TASK-044-upgrade-candidate-hygiene/log.md is inside the reviewed boundary, so the judge reads the implementing agent's own account as evidence. The implementation log belongs at .akrctx/local/impl/TASK-044/log.md. Doctor does not move it; decide whether to move it yourself.
- [warning] TASK-048: .akrctx/tasks/TASK-048-prove-upgrade-candidates-resolved-before-delete/log.md is inside the reviewed boundary, so the judge reads the implementing agent's own account as evidence. The implementation log belongs at .akrctx/local/impl/TASK-048/log.md. Doctor does not move it; decide whether to move it yourself.
- [warning] TASK-049: .akrctx/tasks/TASK-049-support-declared-node20-range/log.md is inside the reviewed boundary, so the judge reads the implementing agent's own account as evidence. The implementation log belongs at .akrctx/local/impl/TASK-049/log.md. Doctor does not move it; decide whether to move it yourself.
- [warning] TASK-050: .akrctx/tasks/TASK-050-honor-implementer-trigger/log.md is inside the reviewed boundary, so the judge reads the implementing agent's own account as evidence. The implementation log belongs at .akrctx/local/impl/TASK-050/log.md. Doctor does not move it; decide whether to move it yourself.
- [warning] TASK-051: .akrctx/tasks/TASK-051-avoid-self-invalidating-review-checklists/log.md is inside the reviewed boundary, so the judge reads the implementing agent's own account as evidence. The implementation log belongs at .akrctx/local/impl/TASK-051/log.md. Doctor does not move it; decide whether to move it yourself.
- [warning] TASK-055: .akrctx/tasks/TASK-055-preserve-validation-failure-evidence/log.md is inside the reviewed boundary, so the judge reads the implementing agent's own account as evidence. The implementation log belongs at .akrctx/local/impl/TASK-055/log.md. Doctor does not move it; decide whether to move it yourself.
- [warning] TASK-056: .akrctx/tasks/TASK-056-centralize-upgrade-candidate-provenance/log.md is inside the reviewed boundary, so the judge reads the implementing agent's own account as evidence. The implementation log belongs at .akrctx/local/impl/TASK-056/log.md. Doctor does not move it; decide whether to move it yourself.
- [warning] TASK-057: .akrctx/tasks/TASK-057-canonicalize-judge-base-refs/log.md is inside the reviewed boundary, so the judge reads the implementing agent's own account as evidence. The implementation log belongs at .akrctx/local/impl/TASK-057/log.md. Doctor does not move it; decide whether to move it yourself.
- [warning] TASK-058: .akrctx/tasks/TASK-058-guard-empty-judge-boundaries/log.md is inside the reviewed boundary, so the judge reads the implementing agent's own account as evidence. The implementation log belongs at .akrctx/local/impl/TASK-058/log.md. Doctor does not move it; decide whether to move it yourself.
- [warning] TASK-059: .akrctx/tasks/TASK-059-release-0-6-0-hardening/log.md is inside the reviewed boundary, so the judge reads the implementing agent's own account as evidence. The implementation log belongs at .akrctx/local/impl/TASK-059/log.md. Doctor does not move it; decide whether to move it yourself.
- [warning] TASK-060: .akrctx/tasks/TASK-060-product-evolution-plan/log.md is inside the reviewed boundary, so the judge reads the implementing agent's own account as evidence. The implementation log belongs at .akrctx/local/impl/TASK-060/log.md. Doctor does not move it; decide whether to move it yourself.
- [warning] TASK-062: .akrctx/tasks/TASK-062-capsule-search-readonly/log.md is inside the reviewed boundary, so the judge reads the implementing agent's own account as evidence. The implementation log belongs at .akrctx/local/impl/TASK-062/log.md. Doctor does not move it; decide whether to move it yourself.
- [warning] TASK-064: .akrctx/tasks/TASK-064-implementar-estado-port-til-de-ejecuci-n-y-lectura-legacy/log.md is inside the reviewed boundary, so the judge reads the implementing agent's own account as evidence. The implementation log belongs at .akrctx/local/impl/TASK-064/log.md. Doctor does not move it; decide whether to move it yourself.
- [warning] TASK-067: .akrctx/tasks/TASK-067-implementar-verificaci-n-completa-de-validaciones-por-comando/log.md is inside the reviewed boundary, so the judge reads the implementing agent's own account as evidence. The implementation log belongs at .akrctx/local/impl/TASK-067/log.md. Doctor does not move it; decide whether to move it yourself.
- [warning] TASK-071: .akrctx/tasks/TASK-071-judge-timings/log.md is inside the reviewed boundary, so the judge reads the implementing agent's own account as evidence. The implementation log belongs at .akrctx/local/impl/TASK-071/log.md. Doctor does not move it; decide whether to move it yourself.
