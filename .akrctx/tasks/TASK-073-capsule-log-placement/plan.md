# Plan

## Workflow
SDD+TDD. The write policy is a shipped contract that agents follow literally, and the Doctor finding is new observable output. Define both first, then write failing template and Doctor tests before implementation.

## Steps
1. Record the contract and the single log location. Resolve the three open questions with the human before code.
2. Red tests: corrected write-policy wording in the three templates, old wording absent, Doctor finding present and absent.
3. Correct the three source templates and the generated policy defaults.
4. Add the Doctor finding for a capsule that holds a `log.md`.
5. Prepare the exact minimal diff for protected root instruction files. Show it and ask for approval before applying. Fall back to the suggested-file path if approval is withheld.
6. Run handoff checks. Complete the checklist before capturing any snapshot, per TASK-051.
7. Offer independent judge review under repository approval rules.
